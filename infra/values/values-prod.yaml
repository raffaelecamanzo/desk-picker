global:
  host: desk-picker.example.com

backend:
  image:
    repository: desk-picker-backend
    tag: latest
  ingress:
    hosts:
      - host: desk-picker.example.com
        paths:
          - path: /api
            pathType: Prefix

frontend:
  image:
    repository: desk-picker-frontend
    tag: latest
  ingress:
    hosts:
      - host: desk-picker.example.com
        paths:
          - path: /
            pathType: Prefix

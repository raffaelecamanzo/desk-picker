global:
  host: desk-picker.dev.example.com

backend:
  image:
    repository: desk-picker-backend
    tag: latest
  ingress:
    hosts:
      - host: desk-picker.dev.example.com
        paths:
          - path: /api
            pathType: Prefix

frontend:
  image:
    repository: desk-picker-frontend
    tag: latest
  ingress:
    hosts:
      - host: desk-picker.dev.example.com
        paths:
          - path: /
            pathType: Prefix
